<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFrameworks>netstandard2.0;net8.0;net9.0;net10.0;</TargetFrameworks>
        <LangVersion>14</LangVersion>
        <ImplicitUsings>disable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Title>EnhancedLinq</Title>
        <Authors>Alastair Lundy</Authors>
        <Copyright>Copyright (c) 2025 Alastair Lundy</Copyright>
        <RepositoryUrl>https://github.com/alastairlundy/Enhancedlinq</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageReleaseNotes>* Updated to DotExtensions 9.1.0 Preview 2 from 9.0.0 (#110)
* Updated to Polyfill 9.3.0 from 9.1.0 ( #110 )
* Fixed a bug where ``IndicesOf`` Immediate mode extensions would return the wrong indices
* Fixed a bug where ``LastIndexOf`` Immediate mode extensions would not return the last index of the element
* Fixed a bug where SplitByItemCountEnumerator failed to execute due to an incorrect state configuration
* Enabled Argument Exception style ThrowIfX Polyfill methods for .NET Standard 2.0
* Converted some Deferred methods to extension blocks</PackageReleaseNotes>
        <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
        <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
        <Description>Additional LINQ style Deferred and Immediate enumeration mode extension methods for .NET . </Description>
        <PackageTags>linq, enhancedlinq, enhanced, deferred-execution,extensions</PackageTags>
        <IncludeSymbols>true</IncludeSymbols>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <SymbolPackageFormat>snupkg</SymbolPackageFormat>
        <EmbedUntrackedSources>true</EmbedUntrackedSources>
        <Version>0.13.0</Version>
        <AssemblyName>AlastairLundy.EnhancedLinq</AssemblyName>
        <PackageId>AlastairLundy.EnhancedLinq</PackageId>
        <RootNamespace>AlastairLundy.EnhancedLinq</RootNamespace>
        <PolyArgumentExceptions>true</PolyArgumentExceptions>
    </PropertyGroup>

    <ItemGroup>
      <EmbeddedResource Update="Internals\Localizations\Resources.resx">
        <Generator>ResXFileCodeGenerator</Generator>
        <LastGenOutput>Resources.Designer.cs</LastGenOutput>
      </EmbeddedResource>
    </ItemGroup>

    <ItemGroup>
      <Compile Update="Internals\Localizations\Resources.Designer.cs">
        <DesignTime>True</DesignTime>
        <AutoGen>True</AutoGen>
        <DependentUpon>Resources.resx</DependentUpon>
      </Compile>
    </ItemGroup>

    <ItemGroup>
      <PackageReference Include="AlastairLundy.DotExtensions" Version="9.1.0-preview.2" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Polyfill" Version="9.3.0" PrivateAssets="all"
                          Condition="'$(TargetFramework)' == 'netstandard2.0'"/>
        <PackageReference Include="Microsoft.Bcl.AsyncInterfaces" Version="10.0.0"
                          Condition="'$(TargetFramework)' == 'netstandard2.0'" />
        <PackageReference Include="System.Linq.AsyncEnumerable" Version="10.0.0" 
                          Condition="'$(TargetFramework)' == 'netstandard2.0' OR '$(TargetFramework)' == 'net8.0' OR '$(TargetFramework)' == 'net9.0'"/>
    </ItemGroup>

    <ItemGroup>
      <Content Include="..\..\README.md">
        <Link>README.md</Link>
          <PackagePath>README.md</PackagePath>
      </Content>
    </ItemGroup>

    <ItemGroup>
      <Folder Include="Async\Deferred\Enumerables\" />
      <Folder Include="Async\Deferred\Enumerators\" />
    </ItemGroup>

</Project>
