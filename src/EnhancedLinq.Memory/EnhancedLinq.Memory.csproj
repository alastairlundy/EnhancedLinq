<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFrameworks>netstandard2.0;net8.0;net9.0;net10.0;</TargetFrameworks>
        <LangVersion>14</LangVersion>
        <ImplicitUsings>disable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Title>EnhancedLinq.Memory</Title>
        <Authors>Alastair Lundy</Authors>
        <Description>LINQ style Immediate enumeration mode extension methods for Spans and Memory. </Description>
        <Copyright>Copyright (c) 2025 Alastair Lundy</Copyright>
        <RepositoryUrl>https://github.com/alastairlundy/EnhancedLinq</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageTags>linq, enhanced-linq, enhanced, extensions, memory, extensions</PackageTags>
        <PackageReleaseNotes>### Memory package
* Converted Immediate ``Select`` extension methods to extension blocks
* Converted Immediate ``None`` extension methods to extension blocks
* Added ``ReadOnlySpan&lt;T&gt;`` Numeric ``Sum``, ``Average``, ``Minimum``, and ``Maximum`` extensions
* Added ``ReadOnlySpan&lt;T&gt;`` Immediate ``GroupBy`` and ``Where`` extensions
* Added ``ReadOnlySpan&lt;T&gt;`` Immediate ``Any`` and ``All`` extensions
* Added ``ReadOnySpan&lt;T&gt;`` Immediate ``Except`` extensions
* Added ``ReadOnlySpan&lt;T&gt;`` Immediate ``CountAtMost`` non-predicate extension and converted predicate extensions to extension block
* Added ``ReadOnlySpan&lt;T&gt;`` Immediate ``CountAtLeast`` predicate and non-predicate extensios in extension blocks
* Fixed a xml doc comment grammar issue
* Updated to DotExtensions 9.2.0 Preview 2 from 9.1.1
* Addressed warnings caused by xml doc comments</PackageReleaseNotes>
        <PackageLicenseExpression>MPL-2.0</PackageLicenseExpression>
        <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <IncludeSymbols>true</IncludeSymbols>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <SymbolPackageFormat>snupkg</SymbolPackageFormat>
        <EmbedUntrackedSources>true</EmbedUntrackedSources>
        <Version>1.0.0-alpha.2</Version>
        <PackageId>EnhancedLinq.Memory</PackageId>
        <PolyArgumentExceptions>true</PolyArgumentExceptions>
        <EnableTrimAnalyzer>true</EnableTrimAnalyzer>
        <TrimmerSingleWarn>false</TrimmerSingleWarn>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="AlastairLundy.DotExtensions" Version="9.2.0-preview.2" />
        <PackageReference Include="AlastairLundy.DotExtensions.Memory" Version="9.2.0" />
        <PackageReference Include="AlastairLundy.DotPrimitives.Collections" Version="4.0.0" />
        <PackageReference Include="Polyfill" Version="9.3.4" PrivateAssets="all"
                          Condition="'$(TargetFramework)' == 'netstandard2.0'"/>
    </ItemGroup>
    
    <ItemGroup>
      <EmbeddedResource Update="Internals\Localizations\Resources.resx">
        <Generator>ResXFileCodeGenerator</Generator>
        <LastGenOutput>Resources.Designer.cs</LastGenOutput>
      </EmbeddedResource>
    </ItemGroup>

    <ItemGroup>
      <Compile Update="Internals\Localizations\Resources.Designer.cs">
        <DesignTime>True</DesignTime>
        <AutoGen>True</AutoGen>
        <DependentUpon>Resources.resx</DependentUpon>
      </Compile>
    </ItemGroup>

    <ItemGroup>
        <Content Include="README.md">
            <Link>README.md</Link>
            <PackagePath>README.md</PackagePath>
        </Content>
    </ItemGroup>
</Project>
